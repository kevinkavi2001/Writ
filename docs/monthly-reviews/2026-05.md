# Writ monthly review -- 2026-05

Reviewer: Lucio
Period: 2026-04-03 through 2026-05-03 (30-day window for most analyzers, 60/90-day where specified)
Friction log line count at start: not captured (first monthly review under this template)
Friction log line count at end: 7002

## Rule effectiveness

`writ analyze-friction --rule-effectiveness --since 30 --json`

| rule_id        | activations | stick rate | rationalizations | action |
|----------------|------------:|-----------:|-----------------:|--------|
| ARCH-TYPE-001  |         124 |        0.0 |                0 | keep   |
| PY-PYDANTIC-001|         106 |        0.0 |                0 | keep   |
| PY-IMPORT-001  |          74 |        0.0 |                0 | keep   |
| PY-ASYNC-001   |          70 |        0.0 |                0 | keep   |
| PERF-IO-001    |          69 |        0.0 |                0 | keep   |
| PHP-ERR-001    |          67 |        0.0 |                0 | keep   |
| TEST-INT-001   |          60 |        0.0 |                0 | keep   |
| ARCH-CONST-001 |          57 |        0.0 |                0 | keep   |
| ARCH-FUNC-001  |          55 |        0.0 |                0 | keep   |
| SEC-UNI-002    |          52 |        0.0 |                0 | keep   |

Notes:

- `denial_stick_rate` is reported as 0.0 across the top 10 rules because
  `stuck_denials` is 0 across the same set. The metric is only meaningful
  when a rule has at least one stuck denial; with zero denials it
  defaults to 0.0 and is not a graduation/trim signal on its own.
- Activations are healthy and rationalizations are zero, which is the
  desired baseline for this window. No rule on this list crossed the
  cadence thresholds (`stick rate < 50%` revise, `>= 85% with low
  rationalization` graduate) because the precondition (denials > 0) was
  not met.

## Skill usage

`writ analyze-friction --skill-usage --since 60`

| skill_id | loads | completion rate | action |
|----------|------:|----------------:|--------|
| (none)   |     - |               - | -      |

Notes: Analyzer returned an empty result set. No `skill_load` /
`skill_complete` events in the 60-day window. Either skills are not yet
emitting these events or no skill ran in the period; nothing to deprecate
this month on this signal.

## Playbook compliance

`writ analyze-friction --playbook-compliance --since 30`

| playbook_id | runs | compliant | common skips | action |
|-------------|-----:|----------:|--------------|--------|
| (none)      |    - |         - |            - | -      |

Notes: Empty result set in the 30-day window. The
`playbook_step_complete` friction event was added in the same uncommitted
working-tree change that wires /dashboard (see below); we will not have
playbook compliance data until those changes land and accrue runs.

## Graduation candidates

`writ analyze-friction --graduation-candidates`

| rule_id | days stable | current | recommended | decision |
|---------|------------:|---------|-------------|----------|
| (none)  |           - | -       | -           | -        |

Decision: **No graduation candidates this month.** The analyzer returned
an empty list. Per the ritual ("not invented"), nothing is being
promoted. Re-run next month once denial signal accrues.

## Trim candidates

`writ analyze-friction --trim-candidates --since 90`

| entity        | type | activations | last seen              | decision               |
|---------------|------|------------:|------------------------|------------------------|
| ARCH-ORG-001  | rule |           4 | 2026-04-24T23:44:03Z   | trim or consolidate    |

Concrete trim candidate: **ARCH-ORG-001**.

- rule_id: `ARCH-ORG-001`
- activations in 90 days: **4** (analyzer threshold: `< 5`)
- last seen: **2026-04-24T23:44:03Z** (within window, so the rule is not
  fully dormant -- it is just under-firing)
- type: rule
- analyzer recommendation: `trim or consolidate`

Action proposed for next month: read ARCH-ORG-001's text and decide
between (a) consolidating into a sibling ARCH-* rule with overlapping
WHEN conditions, or (b) deprecating via SUPERSEDES if no sibling fits.
Not deprecating this month -- 4 activations means the rule is firing on
real triggers, just rarely.

## Quality-judge false positives

`writ analyze-friction --quality-judge-false-positives --since 30`

| rubric | fails | overrides | rate | action |
|--------|------:|----------:|-----:|--------|
| (none) |     - |         - |    - | -      |

Notes: Empty result set. No quality-judge override events in the 30-day
window. No rubric refinement triggered.

## Dashboard check

`curl http://127.0.0.1:8765/dashboard` -> **HTTP 404 ("Not Found")**.

Root cause: the `/dashboard` route is present in the working-tree diff
(`git diff writ/server.py` shows `@app.get("/dashboard", ...)` plus the
`render_dashboard` import) but the running uvicorn process (PID 58573,
started 16:07) was started before that change and has not been
restarted, so it is serving the pre-dashboard server module.
`writ/dashboard.py` itself is also untracked. Not restarting the server
unilaterally; flagging here so the operator can restart and re-curl.

## Decisions taken this month

- No graduation: the analyzer returned no candidates; no promotion.
- No trim executed: ARCH-ORG-001 flagged as the sole trim candidate but
  decision deferred a month pending a read of the rule body and a check
  for a consolidation target.
- No rubric refinement: no quality-judge override signal in the window.

## Action items for next month

- [ ] Restart the writ uvicorn server so the wired `/dashboard` route
      becomes reachable, then re-run the dashboard curl check.
- [ ] Land (commit) the uncommitted Phase 5 work in
      `writ/dashboard.py`, `writ/server.py`, `writ/analysis/friction.py`,
      `writ/cli.py`, and the new `tests/test_phase5_*` files so future
      reviews have stable code under analysis.
- [ ] Read ARCH-ORG-001 and either consolidate it into a sibling
      ARCH-* rule or deprecate via SUPERSEDES.
- [ ] Confirm whether `skill_load` / `skill_complete` events are
      actually emitted anywhere -- the empty skill-usage table may
      indicate missing instrumentation rather than missing usage.
- [ ] Capture friction-log line count at the start of the next review
      window so the start/end delta is meaningful.
